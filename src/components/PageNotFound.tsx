import React from "react";

export default function PageNotFound() {
    return (
        <div style={{backgroundColor: "mediumvioletred", textAlign: "center", fontSize: "40px"}}>
            <header>404: The page you are looking for can't be found.</header>
        </div>
    );
}